import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MedCare - System zarządzania służbą zdrowia",
  description:
    "Nowoczesna platforma do zarządzania wizytami lekarskimi, historią medyczną i komunikacją z lekarzami.",
  keywords:
    "healthcare, medical, appointments, doctors, patients, health management",
  authors: [{ name: "MedCare Team" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pl">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster position="bottom-right" richColors expand closeButton />
      </body>
    </html>
  );
}
